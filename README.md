# roboshop-dockerfiles

Purpose: Dockerfiles, compose and service folders for Roboshop microservices.

Key files: docker-compose.yaml, cart/, catalogue/, frontend/, mongodb/, mysql/, payment/, rabbitmq/, redis/, shipping/, user/
